using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testSpells : MonoBehaviour {

    void Start()
    {
        wiz_control.lullabyStock = 9;
        wiz_control.blanketStock = 7;
        wiz_control.drainStock = 1;
        wiz_control.warmMilkStock = 1;
        wiz_control.eternalSleepStock = 0;
    }

    // Update is called once per frame
    void Update () {
		
	}
}
